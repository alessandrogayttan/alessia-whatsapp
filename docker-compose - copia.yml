services:
  alessia:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./agente-inpulso-bda72425fab5.json:/app/agente-inpulso-bda72425fab5.json:ro
      - ./precios.json:/app/precios.json:ro
    restart: unless-stopped
