services:
  alessia:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./precios.json:/app/precios.json:ro
    restart: unless-stopped
